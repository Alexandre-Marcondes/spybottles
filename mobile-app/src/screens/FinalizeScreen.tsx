import React from 'react';
import { View, Text } from 'react-native';

const FinalizeScreen = () => {
  return (
    <View>
      <Text>Finalize Session</Text>
    </View>
  );
};

export default FinalizeScreen;
