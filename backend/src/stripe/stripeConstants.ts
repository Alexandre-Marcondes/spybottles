export const STRIPE_API_VERSION = '/v1.0.0';

export const STRIPE_PREFIX = '/stripe';
export const STRIPE_ROUTE = STRIPE_API_VERSION + STRIPE_PREFIX;

export const STRIPE_WEBHOOK_PREFIX = '/webhook';
export const STRIPE_WEBHOOK_PATH = STRIPE_ROUTE + STRIPE_WEBHOOK_PREFIX;
